# msgtree: Simple Message Tree REST API

### Quick Start
```shell
npm i
npm test
rm -rf node_modules
npm i --prod
npm start
```

Also install packages globally:
```shell
sudo npm install -g eslint istanbul
```
